{
    "tasks": [
        {
            "type": "shell",
            "label": "g++ build active file",
            "command": "make",
            "args": [
                "CC_FLAGS=-g",
                "TARGET=${fileBasenameNoExtension}"
            ],
            "options": {
                // "cwd": "/usr/bin"
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}
